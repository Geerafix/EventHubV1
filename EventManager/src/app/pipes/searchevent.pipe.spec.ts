import { SearchEventPipe } from './searchevent.pipe';

describe('SearchEventPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEventPipe();
    expect(pipe).toBeTruthy();
  });
});
